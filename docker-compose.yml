services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./processing_data:/processing_data
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
